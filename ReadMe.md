How to start the application:

start Spring Boot application by running the main method in the `SpringThymeleafApplication` class.

open www.localhost/8080 in your browser

You'll see the home page with a welcome message and a button.
Click the button to navigate to the medication list, where you can add, edit and delete medications from the list.